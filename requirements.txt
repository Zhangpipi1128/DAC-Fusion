# Core (required for kaggle.py)
torch>=1.10.0
transformers>=4.30.0
pandas>=1.3.0
numpy>=1.21.0
tqdm>=4.62.0

# Significance tests (Wilcoxon in significance_tests*.csv; optional but recommended)
scipy>=1.7.0

# Sentence-BGE baseline — needed when RUN_ST_BASELINE=1 or VULN_IR_FULL_PAPER_RUN=1
# (kaggle_notebook_cell1.py can also: pip install sentence-transformers)
sentence-transformers>=2.2.0

# Notes:
# - BM25 is implemented in kaggle.py (BM25OkapiLite).
# - SCRUBD data: clone via kaggle_notebook_cell1.py, not pip.
# - LLM rerank (RUN_LLM_RERANK_BASELINE=1): needs extra GPU memory; uses transformers only.
# - Kaggle notebooks often pre-install torch/transformers; local users should use CUDA-enabled torch if available.
